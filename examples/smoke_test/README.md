# Smoke test

This directory is reserved for the output of the small Stage-7 smoke run. The
output itself is ignored by Git so that repeated local checks do not pollute the
repository history.

Run the command in `docs/reproduction.md` from the repository root.
